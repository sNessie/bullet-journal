const categoriesDefaultState = [];

const categoriesReducers = (state = categoriesDefaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default categoriesReducers;
